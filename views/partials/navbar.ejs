<% if (currentUser.role === "professional"){ %>
<nav>
<a href="/profile/<%= currentUser._id %>">Profile</a>
<a href="/profile/<%= currentUser._id %>/edit">Edit info</a>
<form action="/logout" method="post">
  <input type="submit" value="Log out">
</form>
</nav>
<% } else if ( currentUser.role === "client"){ %>
<nav>
  <a href="/profile/<%= currentUser._id %>">Profile </a>
  <a href="/profile/<%= currentUser._id %>/favorite">My fav. Stores </a>
  <a href="/profile/<%= currentUser._id %>/add">Add a Store </a>
  <form action="/logout" method="post">
    <input type="submit" value="Log out">
  </form>
</nav>
<% } %>
